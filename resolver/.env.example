# OneGrid Resolver Bot Configuration
# Copy to .env and fill in values

# Admin keypair (base64 Ed25519) — generate with: node keygen.mjs
ADMIN_SECRET_KEY=

# Deployed contract addresses — get from deploy output
PACKAGE_ID=0xTODO
GRID_OBJECT_ID=0xTODO
ADMIN_CAP_ID=0xTODO

# RPC endpoint (default: OneChain testnet)
RPC_URL=https://rpc-testnet.onelabs.cc:443
